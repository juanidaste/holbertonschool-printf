#ifndef MAIN_C
#define MAIN_C

#include <unistd.h>
#include <stdarg.h>
#include <string.h>
int printc(int c);
void printporcentaje(int c);
int _printf(const char *format, ...);
int printstring(char *str);

#endif
